<%= form_with(model: order_product, local: true) do |form| %>
  <% if order_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_product.errors.count, "error") %> prohibited this order_product from being saved:</h2>

      <ul>
      <% order_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quntity %>
    <%= form.number_field :quntity %>
  </div>

  <div class="field col-xs-12" >
      <%= form.label :product_id %>
      <%= form.select :product_id, options_for_select(Product.all.collect{|c| [c.name,c.id]}) %>
  </div>   


  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
